[build]
    # Build from monorepo root using Turborepo
    base = "apps/studio"
    command = "pnpm run build --filter=@chimborazo/studio"
    publish = "dist"

[build.environment]
    NODE_VERSION = "22"

[[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
