# ============================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# ============================================
# These variables are only accessible on the server (process.env)
# and are NOT exposed to the client browser.

# Server URL (optional)
# Used for sitemap generation and absolute URLs
SERVER_URL=

# Sanity API Token (optional)
# Required for: mutations, preview mode, and revalidation webhooks
# Create a token with Editor or Admin permissions at:
# https://sanity.io/manage/project/[PROJECT_ID]/api
SANITY_API_TOKEN=

# Sanity Webhook Secret (optional)
# Used to validate incoming webhook requests from Sanity
SANITY_WEBHOOK_SECRET=

# Netlify Configuration (optional)
# Used for cache purging via Netlify API
# Find Site ID in: Netlify Dashboard > Site Settings > General
# Get token from: https://app.netlify.com/user/applications#personal-access-tokens
NETLIFY_AUTH_TOKEN=
NETLIFY_SITE_ID=

# Anthropic API Key (optional)
# Required for AI-powered image metadata generation
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# ============================================
# CLIENT-SIDE ENVIRONMENT VARIABLES
# ============================================
# These variables are prefixed with VITE_ and are exposed to the browser.
# DO NOT put secrets here!

# App Title (optional)
VITE_APP_TITLE=

# Sanity Configuration (required)
# Get these from: https://sanity.io/manage/project/[PROJECT_ID]
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
