node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.output
.vinxi
todos.json
.worktrees/

# Turborepo cache
.turbo

# Local Netlify folder
.netlify

storybook-static
*.tsbuildinfo


# Claude Code: share project config and agents, keep personal overrides local.
# settings.local.json holds machine-specific paths and per-user permissions.
.claude/settings.local.json
.claude/*.local.json
.playwright-mcp
.omc

# Sanity extracted schema — regenerate with `pnpm --filter @chimborazo/sanity-config schema:generate`
packages/sanity-config/schema.json

# Local pnpm store / tool scratch dirs
.pnpm-store
.lavish
