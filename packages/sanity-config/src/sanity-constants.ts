/**
 * Shared project/dataset identifiers used by CLI and studio tooling.
 * Keeping them here prevents `apps/studio/sanity.cli.ts` and
 * `packages/sanity-config/sanity.{cli,config}.ts` from drifting.
 */
export const SANITY_PROJECT_ID = "pntpob7k"
export const SANITY_DATASET = "production"
